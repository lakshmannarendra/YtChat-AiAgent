import * as parse from './lib/parse';
declare const _default: {
    getScheduleDescription(p: parse.ParsedCron): string;
    prev(parsed: parse.ParsedCron, from: Date): Date | null;
    next(parsed: parse.ParsedCron, from: Date): Date | null;
    parse(cron: string): parse.ParsedCron;
};
/**
 * cron is assumed to be validated by AWS already
 * AWS Cron Expression specs
 * https://docs.aws.amazon.com/AmazonCloudWatch/latest/events/ScheduledEvents.html#CronExpressions
 */
export = _default;

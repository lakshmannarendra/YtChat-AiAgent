export default class BundledCode {
    constructor(path, className, functionNames, dependencies) {
        this.path = path;
        this.className = className;
        this.functionNames = functionNames;
        this.dependencies = dependencies;
    }
}

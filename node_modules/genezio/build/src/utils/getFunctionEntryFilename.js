import { entryFileFunctionMap } from "../projectConfiguration/yaml/models.js";
export function getFunctionEntryFilename(language, filename) {
    const extension = entryFileFunctionMap[language].split(".")[1];
    return `${filename}.${extension}`;
}

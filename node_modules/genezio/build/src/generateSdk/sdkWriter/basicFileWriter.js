import { replaceUrlsInSdk, writeSdkToDisk } from "../../utils/sdk.js";
export async function basicFileWriter(sdk, classUrls, path) {
    await replaceUrlsInSdk(sdk, classUrls);
    await writeSdkToDisk(sdk, path);
    return path;
}
